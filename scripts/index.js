const scooter = document.querySelector('.main .landing .center .background-image')

setTimeout(() => {
    scooter.style.transform = "translateX(0vw)"    
    scooter.style.opacity = "1"
}, 50);

const wheel = document.querySelector('.main .landing .center .background-imagen')

setTimeout(() => {
    wheel.style.transform = "translateX(30vw)"    
    wheel.style.opacity = "1"
}, 50);
